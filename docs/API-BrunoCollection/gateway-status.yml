info:
  name: gateway-status
  type: http
  seq: 10

http:
  method: GET
  url: "{{host}}/api/v1/gateway/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
