info:
  name: Get import by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/imports/{{import_job_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
