info:
  name: Logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/v1/auth/logout"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
