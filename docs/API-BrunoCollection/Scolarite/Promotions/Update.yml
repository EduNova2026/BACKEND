info:
  name: Update promotion
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/api/v1/scolarite/promotions/{{promotion_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
        "nom": "ISEN-CIR3",
        "annee_scolaire": "2026-2027"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
