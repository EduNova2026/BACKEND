info:
  name: List promotion groupes
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/promotions/{{promotion_id}}/groupes"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
