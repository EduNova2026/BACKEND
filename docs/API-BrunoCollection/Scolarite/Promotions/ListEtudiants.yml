info:
  name: List promotion etudiants
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/promotions/{{promotion_id}}/etudiants"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
