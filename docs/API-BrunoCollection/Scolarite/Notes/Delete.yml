info:
  name: Delete note
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/v1/scolarite/notes/{{note_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
