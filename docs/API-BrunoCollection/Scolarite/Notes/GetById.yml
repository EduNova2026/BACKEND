info:
  name: Get note by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/notes/{{note_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
