info:
  name: List responsables by promotion
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/promotions/{{promotion_id}}/responsables"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
