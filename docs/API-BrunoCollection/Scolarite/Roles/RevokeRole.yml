info:
  name: Revoke role
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/api/v1/scolarite/roles/utilisateurs/{{utilisateur_id}}/roles/{{role_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
