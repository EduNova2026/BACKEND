info:
  name: List roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/roles/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
