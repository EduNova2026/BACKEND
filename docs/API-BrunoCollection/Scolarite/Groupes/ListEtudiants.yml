info:
  name: List groupe etudiants
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/groupes/{{groupe_id}}/etudiants"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
