info:
  name: Delete groupe
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/api/v1/scolarite/groupes/{{groupe_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
