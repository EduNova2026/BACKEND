info:
  name: Get groupe etudiants moyennes
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/groupes/{{groupe_id}}/etudiants/moyennes?semestre=1"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
