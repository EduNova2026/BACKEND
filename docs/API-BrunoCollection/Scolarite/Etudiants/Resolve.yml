info:
  name: Resolve etudiant for CSV
  type: http
  seq: 9

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/etudiants/resolve?promotion_id={{promotion_id}}&nom=dupont&prenom=elodie"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
