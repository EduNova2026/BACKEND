info:
  name: List etudiants
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/etudiants/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
