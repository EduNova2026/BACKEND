info:
  name: Export etudiant relevé
  type: http
  seq: 13

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/etudiants/{{etudiant_id}}/export"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
