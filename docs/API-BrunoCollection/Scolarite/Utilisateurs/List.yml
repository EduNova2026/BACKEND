info:
  name: List utilisateurs
  type: http
  seq: 0

http:
  method: GET
  url: "{{host}}/api/v1/scolarite/utilisateurs/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5