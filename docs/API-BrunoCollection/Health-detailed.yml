info:
  name: Health-detailed
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/health/detailed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
